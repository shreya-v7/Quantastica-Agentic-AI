/**
 * Stable ID written to localStorage by the dev-only `/dev/session` route.
 * Production builds never expose a working bypass (see `DevSessionEntry.tsx`).
 */
export const DEV_DUMMY_USER_ID = "dev-dummy-user-0001";
